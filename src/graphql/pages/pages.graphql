query getPages($first: Int) {
  pages(first: $first) {
    heading
    id
    slug
    body {
      html
    }
  }
}

query getPageBySlug($slug: String!) {
  page(where: { slug: $slug }) {
    id
    slug
    heading
    body {
      html
    }
  }
}
